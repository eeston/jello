// TODO: testing components with react-native-unistyles is not working
// https://github.com/jpudysz/react-native-unistyles/discussions/88

// import { render, fireEvent } from "@testing-library/react-native";

// import { Button } from "./Button";

describe.skip("Button", () => {
  it.skip("should render the button title", () => {
    //     const { getByText } = render(
    //       <Button title="Test Button" onPress={() => {}} />,
    //     );
    //     expect(getByText("Test Button")).toBeTruthy();
  });
  //   it("should call onPress when the button is pressed", () => {
  //     const onPress = jest.fn();
  //     const { getByText } = render(
  //       <Button title="Test Button" onPress={onPress} />,
  //     );
  //     fireEvent.press(getByText("Test Button"));
  //     expect(onPress).toHaveBeenCalled();
  //   });
  //   it("should render an activity indicator when isLoading is true", () => {
  //     const { getByTestId } = render(
  //       <Button title="Test Button" onPress={() => {}} isLoading />,
  //     );
  //     expect(getByTestId("activity-indicator")).toBeTruthy();
  //   });
  //   it("should be disabled when disabled is true", () => {
  //     const onPress = jest.fn();
  //     const { getByText } = render(
  //       <Button title="Test Button" onPress={onPress} disabled />,
  //     );
  //     fireEvent.press(getByText("Test Button"));
  //     expect(onPress).not.toHaveBeenCalled();
  //   });
});
